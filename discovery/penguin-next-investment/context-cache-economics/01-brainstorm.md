# ① 头脑风暴 · context-cache-economics

- 技能：`bmad-brainstorming`，**自主模式**（用户「你决定下」即选定由我跑完全程）｜无 `_bmad/` 脚本 → 无 memlog、无 composer 输出（与发现循环"未过门不落盘"一致）
- 约束（第 0 步事实）：事实 2/3（31% 整段未命中、该会话 54%）、事实 5（产品自述"前缀在一个 Trace 文件内字节固定，以便缓存命中"）、事实 6（本仓库无亲和字段）、事实 7（请求实际由 agenthub 发出，是否带键未知）

## 候选方向（12 个，按 影响面 × 可行性 初排）

| # | 方向 | 影响面 | 可行性 | 一句话 |
|---|---|---|---|---|
| 1 | **请求前缀指纹审计**：发请求前记录 `sha256(rendered prefix)`+长度+影响缓存的参数，写入 Trace | 高 | 高 | 一次记录即可判定"是我们动了前缀，还是上游丢/轮换缓存" |
| 2 | 会话亲和键（`prompt_cache_key` / 网关 sticky）按 session 固定 | 中高 | 中（取决于网关） | 只对"多后端轮换"有效 |
| 3 | 把 strict 层"字节固定"从文档声明升级为**可验证不变量**（违反即标记） | 高 | 高 | 文档已声明，代码未验证 |
| 4 | Trace 每轮记录命中率与未命中归因 | 中 | 高 | 事后定位靠它 |
| 5 | **指标拆分**：冷启动 0% 与热会话整段丢 0% 分开显示 | 中高 | 高 | 现在 47% 会被读成"缓存坏了" |
| 6 | **成本口径**：命中率旁显示"本轮重复处理的 input tokens / 折算价格" | 中高 | 高 | 命中率下降未必更贵（OpenAI 官方明示） |
| 7 | 上下文规模预算（软上限 + 主动 compaction 阈值） | 中 | 中 | 单会话已到 20 万，未命中即全价 |
| 8 | 工具结果裁剪/摘要，压小每轮尾部增量 | 中 | 中 | 降低单次未命中的绝对代价 |
| 9 | thinking level 变更的缓存代价显式化 | 中 | 高 | UI 目前只提示"建议先压缩" |
| 10 | 按模型统计命中率 | 低中 | 高 | `luna` 43% vs `terra` 75%，差异待解释 |
| 11 | 对接上游诊断能力（OpenAI Prompt Cache Diagnostics / Dashboard） | 低中 | 中 | 平台侧已有，产品侧只需引导 |
| 12 | 每轮命中率进 Trace 面板列 | 低 | 高 | Trace 已有整体命中率 |

## 收敛到 Top 5（进入 ② 侦察）

`1 前缀指纹审计`、`2 会话亲和键`、`5 指标拆分`、`6 成本口径`、`7 上下文预算`。
