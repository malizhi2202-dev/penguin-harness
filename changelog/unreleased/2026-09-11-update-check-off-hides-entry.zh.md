# 关闭新版本检查后，账号菜单里不再留入口

`PENGUIN_UPDATE_CHECK=off` 过去只停掉查询本身：账号菜单里那一行还在，点开是一个既没有版本可比较、也没有发布页链接的弹窗（`disabled: true` 的响应里两者都是 null），等于让读者自己去 GitHub 看。

现在这一行随查询一起消失。这也让它成为一处彻底的“无对外通信”部署：那一行正是应用发起唯一一个自发外部请求的入口，入口没了，触发的路径也就不存在了。

默认行为不变：未设置该变量时照旧查询、照旧显示入口。
